name: Keep Render Backend Awake

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call backend
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://superheroes-bj5n.onrender.com/health || true
